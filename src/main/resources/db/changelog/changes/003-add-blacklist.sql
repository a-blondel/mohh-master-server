ALTER TABLE public.ACCOUNT ADD COLUMN IS_BANNED BOOLEAN DEFAULT FALSE;

CREATE TABLE IF NOT EXISTS BLACKLIST (
    ID bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,
    IP varchar(255) NOT NULL
);
